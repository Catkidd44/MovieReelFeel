# MovieReelFeel
MovieReel synthesizes unstructured data using a "detective" logic to scrape IMDb reviews and integrate YouTube APIs. It employs TF-IDF, BERT, and Cosine Similarity to build a ranking engine. The system calculates a final score based on custom user weightings, turning raw sentiment into personalized insights
